# Decision Log

本表記錄專案選擇及其證據邊界。`APPROVED_BY_USER`只表示使用者已批准本專案採用該設定，不表示它已成為原作者CIFAR實驗的`EXPLICIT`事實。任何後續變更都必須新增或更新decision、重新批准並更新frozen config hash。

| Decision ID | Topic | Evidence | Decision | Status | Consequence |
| --- | --- | --- | --- | --- | --- |
| DEC-INIT-001 | Convolution weight distribution | [13] Section 2.2 Eq. (10)/(14)：zero-mean Gaussian，std `sqrt(2/n)` | 所有 convolution weight 使用 zero-mean normal；不得改成 uniform | APPROVED_BY_USER | 鎖定 distribution，但 fan mode 由下一項另決定 |
| DEC-INIT-002 | Convolution fan mode | [13] 同時允許 fan-in Eq. (10) 與 fan-out Eq. (14)；Caffe b590 `MSRAFiller` 預設 fan-in | 採 fan-in，即 std `sqrt(2/fan_in)`；在報告中明列為專案選擇，不稱為作者實際 fan mode | APPROVED_BY_USER | 解除實作歧義；若作者實際用 fan-out，初始尺度會不同 |
| DEC-INIT-003 | Convolution bias initialization/existence | [13] derivation設 `b=0`；作者 ImageNet deploy convolution 為 `bias_term:false`；CIFAR直接證據缺失 | BN 前 convolution 不建立 bias parameter；若未來找到作者 CIFAR prototxt則重新審查 | APPROVED_BY_USER | 影響 parameter count 與 WD scope；目前有旁證但非完整確認 |
| DEC-FC-001 | FC weight initialization | [13]沒有可直接移植的CIFAR single-FC recipe；std `0.01`來自特定ImageNet FC，不是CIFAR設定 | 10-way FC weight使用zero-mean normal、standard deviation=`0.01`；FC bias存在且初始化為0。此設定為`LOW_CONFIDENCE_ASSUMPTION`，不得稱為原作者CIFAR明示設定 | APPROVED_BY_USER | frozen config與報告必須保留evidence status；若找到作者CIFAR training prototxt須重審；不得因test accuracy不佳而更換 |
| DEC-FCBIAS-001 | FC bias existence/initialization | 作者ImageNet deploy的FC依Caffe default含bias；Caffe bias filler預設zero；CIFAR直接證據缺失 | 10-way FC保留bias並初始化為0，明列為project assumption | APPROVED_BY_USER | 與FC weight初始化分離追蹤；影響parameter count與weight-decay scope |
| DEC-SHORT-001 | Option A tensor semantics | 原論文只確認 stride 2、extra zero entries、無參數；官方 CIFAR source缺失 | 空間用固定偶數索引`::2`下採樣，再在channel維度前後對稱補零（16→32各8；32→64各16）；odd-size input明確報錯 | APPROVED_BY_USER | 精確index/padding與odd-size policy屬project assumption，須dedicated value tests |
| DEC-CONV-001 | `3x3` convolution padding | CIFAR shape constraint；作者 ImageNet deploy 的 `3x3` conv均 `pad:1` | 所有 `3x3` convolution使用 padding 1；stage transition由 residual branch第一個 conv stride 2 | APPROVED_BY_USER | 維持 feature maps `[32,16,8]` |
| DEC-BN-001A | BN execution、affine與evaluation policy | 原論文每個conv後使用BN；作者deploy採BatchNorm+Scale；Caffe defaults為gamma1/beta0、eps1e-5、TEST使用stored statistics；作者two-GPU CIFAR細節不可得 | single GPU、non-SyncBN、affine enabled、gamma=1、beta=0、eps=`1e-5`；evaluation使用running statistics；不做post-training recalibration。single GPU與non-SyncBN是project decision，不是作者two-GPU CIFAR設定 | APPROVED_BY_USER | 明確揭露相對於paper two-GPU execution的未知/偏離；不包含running-statistics momentum證據判定 |
| DEC-BN-001B | BN running-statistics momentum | Caffe`moving_average_fraction=0.999`已確認；PyTorch momentum定義與Caffe係數互補 | BN update coefficient固定為PyTorch-style`momentum=0.001`，對應Caffe`.999`；scale-factor與evaluation語意由`DEC-BN-001C`明確規定 | APPROVED_BY_USER | 不得改回PyTorch預設`0.1`；仍須通過mandatory compatibility test |
| DEC-BN-001C | Caffe-compatible scaled-accumulator BatchNorm | 標準PyTorch BatchNorm從`running_var=1`開始且不在eval套用Caffe scale factor；固定小型tensor已證明其early-running/eval語意與Caffe b590不同。這是framework compatibility修正，不是paper CIFAR明示設定 | 採scaled running-mean accumulator、scaled unbiased running-variance accumulator及running scale factor；evaluation使用`accumulator / scale factor`；checkpoint保存`running_mean`、`running_var`、`running_scale`與`num_batches_tracked`全部buffers；compatibility test為正式訓練gate | APPROVED_BY_USER | 專案使用`CaffeCompatibleBatchNorm2d`；不得以標準PyTorch BN或省略scale buffer替代；PASS只證明預註冊framework semantics，不升格為paper EXPLICIT |
| DEC-WD-001 | Weight decay scope — caffe-default-derived candidate | Caffe b590 `decay_mult`預設1，原作者 CIFAR overrides不可得 | `caffe-default-derived candidate`：weight decay `0.0001`套用所有learnable convolution/FC weights、FC bias、BN gamma與BN beta；不存在的conv bias不適用。不得稱為paper-faithful fact | APPROVED_BY_USER | 只是由Caffe default推導的project assumption，可能與作者自有training code不同 |
| DEC-PRE-001 | Pixel scale與mean artifact | Caffe default為 uint8 `0..255`、scale1、先減 mean；官方 Caffe CIFAR example從train LMDB產生同 shape mean並供train/test共用 | 保留`[0,255]`float尺度；以完整50k official training set計算`3x32x32`mean image；train/test使用同一artifact；不做std normalization | APPROVED_BY_USER | 完整50k mean來源仍是project assumption，不是論文明示 |
| DEC-AUG-001 | Geometric augmentation order | [24]明示zero pad 4、crop與random flip；ResNet明示random`32x32`crop | 幾何子序列固定為constant-zero pad 4→random`32x32`crop→random horizontal flip；完整transform中mean的位置由`DEC-MEANORDER-001`決定 | APPROVED_BY_USER | zero padding發生於mean-centered image；幾何程序不得依test結果更換 |
| DEC-MEANORDER-001 | Mean subtraction relative to augmentation | ResNet只明示per-pixel mean subtraction與augmentation，沒有提供兩者確切相對順序；作者CIFAR script不可得 | Training：保持`[0,255]`float→減完整50k training-set的`3x32x32`mean→constant-zero pad 4→random crop 32→random horizontal flip。Test：保持同尺度→減同一mean，不做其他augmentation | APPROVED_BY_USER | 為保留per-pixel mean座標語意採用的project assumption；padding值0代表centered zero；不得稱為作者pipeline已確認 |
| DEC-LR-001 | Project LR boundary candidate | Caffe b590 `iter_`從0開始，ApplyUpdate後遞增；multistep於`iter_ >= stepvalue`在更新前切換；這只確認framework semantics | 專案採#1–32,000用0.1、#32,001–48,000用0.01、#48,001–64,000用0.001，#64,000完成後保存final | APPROVED_BY_USER | 相同boundary是project assumption，不是原作者CIFAR設定已直接證實；須boundary tests |
| DEC-CAFFE-001 | Caffe證據的適用邊界 | 作者README明示models由自有實作轉換至Caffe b590且非以該版Caffe訓練；repository無CIFAR train files | 只把Caffe b590當作framework semantics與候選實作依據；不得把其defaults標成作者CIFAR實驗事實 | APPROVED_BY_USER | 防止證據越界；所有相關設定保留paper evidence status或project-decision標記 |
| DEC-SHUFFLE-001 | Training/test DataLoader sampling policy | ResNet CIFAR論文未明確交代shuffle、sampler或epoch boundary的實作；為確定性且可恢復的SGD minibatch training，專案必須顯式固定sampling policy | Training DataLoader使用`shuffle=true`；每完成一次完整training-set traversal後重新產生排列；使用由base seed確定性派生的`torch.Generator`；generator state必須保存於checkpoint並可恢復；不使用replacement sampling。Test DataLoader使用`shuffle=false`、保持official dataset order且不使用random sampler | APPROVED_BY_USER | 此設定為project assumption，不是論文明示；不得依test accuracy改變sampling policy |
| DEC-RNG-002 | Per-sample augmentation RNG與consumed-cursor resume | DataLoader worker prefetch會使sampler issued cursor領先optimizer實際消耗位置；只保存generator state無法保證mid-epoch exact resume。固定worker stream也會因worker assignment、prefetch與重啟而改變augmentation。Paper未指定這些tensor/runtime細節 | Stochastic augmentation seed由base seed、epoch、official sample index派生，與worker assignment及prefetch順序無關；Dataset必須顯式持有epoch；training batch必須攜帶official indices；只有optimizer update成功後才呼叫`mark_batch_consumed`；checkpoint保存sampler permutation、consumed cursor、epoch及generator state | APPROVED_BY_USER | 此為project reproducibility decision，不是paper EXPLICIT；未消耗prefetch在resume後必須重發，且同identity augmentation逐值一致 |

`docs/reproduction_spec.md` 本次沒有修改，因此沒有需要記錄的 spec correction。
